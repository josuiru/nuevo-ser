# nuevo_ser_tutor

Cliente del Tutor IA — proxy a Claude API con caché LRU + TTL, doble capa de filtros (cliente/servidor) y disparador adaptativo.

## Estado

**Vacío.** Extracción desde `apps/uno-roto/lib/dominio/tutor/` y `apps/uno-roto/lib/datos/{cliente_tutor,cliente_tutor_panel,cache_tutor}.dart` se ejecuta en el Chunk 5 del refactor.

## Licencia

AGPL-3.0.
