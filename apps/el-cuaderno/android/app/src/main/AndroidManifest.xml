<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- A3 (foto vía image_picker): la cámara es necesaria para que el
         niño pueda anclar una foto a su observación (biblia §3.2).
         READ_MEDIA_IMAGES habilita seleccionar de la galería en Android
         13+ (API 33+). En Android 12 y anteriores image_picker usa
         READ_EXTERNAL_STORAGE bajo capó, pero no la declaramos
         explícitamente porque solo Android 13+ es nuestro target del
         piloto. La foto seleccionada se mueve al directorio privado de
         la app (`getApplicationDocumentsDirectory()`) y nunca cruza
         red salvo opt-in del adulto (frontera de privacidad estructural,
         política §2). -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <!-- B5 (geolocator): permiso "solo en uso", nunca background. La
         biblia §2.1 prohíbe tracking continuo. ACCESS_FINE_LOCATION da
         GPS preciso (~5 m); ACCESS_COARSE_LOCATION es el fallback en
         dispositivos sin GPS o cuando el niño elige precisión gruesa.
         Las coordenadas NUNCA cruzan red — se usan localmente para
         derivar `region_code` (NUTS aproximado) y "¿estás en tu sit
         spot?". La pantalla de pre-permiso explica esto al niño. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:label="el_cuaderno"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
