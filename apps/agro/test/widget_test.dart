// Stub generado por flutter create. Sustituir cuando haya tests reales
// de los widgets de la app (mapa, ficha de planta, alta de planta).
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(1 + 1, 2);
  });
}
